import { DBMSQuizQuestion } from './dbmsQuizQuestionsUnit1';

export const UNIT_5_QUIZ_QUESTIONS: DBMSQuizQuestion[] = [
  {
    id: 'dbms-q5-01',
    unitId: 'unit-5',
    unitNumber: 5,
    unitTitle: 'UNIT V – Concurrency Control & Disk Storage',
    topic: 'Lock Compatibility Matrix',
    difficulty: 'Easy',
    conceptTag: 'Shared vs Exclusive Locks',
    question: 'Under standard database locking protocols, which combination of lock requests on the same data item Q can be granted concurrently to two different transactions?',
    options: [
      'Two Exclusive (X) locks',
      'One Shared (S) lock and one Exclusive (X) lock',
      'Two Shared (S) locks',
      'An Exclusive (X) lock and any upgrade lock',
    ],
    correctIndex: 2,
    correctExplanation: 'Multiple transactions can hold Shared (S) locks simultaneously on the same item because reading does not modify data. An Exclusive (X) lock is incompatible with all other locks (both S and X) to prevent dirty reads and lost updates.',
    optionExplanations: [
      'Two Exclusive locks cannot be granted simultaneously; write-write conflicts cause lost updates.',
      'Shared and Exclusive locks conflict; a transaction cannot modify an item while another transaction is reading it.',
      'Two Shared (S) locks is correct because concurrent read operations do not interfere with each other.',
      'An Exclusive lock conflicts with any other lock request on the same item.',
    ],
  },
  {
    id: 'dbms-q5-02',
    unitId: 'unit-5',
    unitNumber: 5,
    unitTitle: 'UNIT V – Concurrency Control & Disk Storage',
    topic: 'Two-Phase Locking (2PL) Protocol',
    difficulty: 'Medium',
    conceptTag: 'Basic 2PL Rules',
    question: 'What is the governing rule of the standard Two-Phase Locking (2PL) protocol?',
    options: [
      'A transaction must acquire all required locks within 2 seconds of starting',
      'Once a transaction releases a single lock, it enters its Shrinking Phase and can NEVER acquire any new lock thereafter',
      'Every transaction must execute its read phase and write phase twice for redundancy',
      'Locks must be requested in increasing order of primary key values',
    ],
    correctIndex: 1,
    correctExplanation: '2PL mandates two distinct phases: (1) Growing phase where locks may be acquired but not released, and (2) Shrinking phase where locks may be released but no new locks can be acquired. This guarantees conflict serializability.',
    optionExplanations: [
      '2PL refers to two logical phases (growing and shrinking), not a two-second wall-clock timeout.',
      'Once a lock is released, no new lock can be acquired is correct; releasing a lock marks the irreversible beginning of the shrinking phase.',
      '2PL does not execute operations twice; the two phases refer to lock acquisition and lock release.',
      'Requesting locks in increasing primary key order is a deadlock avoidance technique (ordered resource allocation), not the definition of basic 2PL.',
    ],
  },
  {
    id: 'dbms-q5-03',
    unitId: 'unit-5',
    unitNumber: 5,
    unitTitle: 'UNIT V – Concurrency Control & Disk Storage',
    topic: 'Strict Two-Phase Locking (Strict 2PL)',
    difficulty: 'Medium',
    conceptTag: 'Strict 2PL',
    question: 'What crucial operational guarantee does Strict 2PL provide that basic 2PL fails to guarantee?',
    options: [
      'Strict 2PL completely eliminates deadlocks',
      'Strict 2PL avoids cascading aborts (ensures cascadeless/strict execution) by holding all Exclusive locks until the transaction commits or aborts',
      'Strict 2PL guarantees zero CPU overhead',
      'Strict 2PL eliminates the need for transaction logs',
    ],
    correctIndex: 1,
    correctExplanation: 'Under Strict 2PL, all Exclusive (X) locks acquired by a transaction must be held until that transaction terminates (COMMIT or ABORT). This prevents any other transaction from reading uncommitted modifications, preventing cascading aborts.',
    optionExplanations: [
      'Strict 2PL does NOT eliminate deadlocks; deadlocks can still occur when transactions wait cyclically for locks held by each other.',
      'Strict 2PL avoids cascading aborts by holding exclusive locks until termination is correct; this is why virtually all commercial relational DBMSs implement Strict 2PL.',
      'Strict 2PL still requires lock manager data structures and CPU checks.',
      'Transaction logs are still mandatory for durability and crash recovery.',
    ],
  },
  {
    id: 'dbms-q5-04',
    unitId: 'unit-5',
    unitNumber: 5,
    unitTitle: 'UNIT V – Concurrency Control & Disk Storage',
    topic: 'Deadlock Detection & Wait-For Graph (WFG)',
    difficulty: 'Easy',
    conceptTag: 'Wait-For Graph',
    question: 'How does a centralized DBMS detect that a deadlock has occurred among active concurrent transactions?',
    options: [
      'By checking if disk usage exceeds 95% capacity',
      'By periodically constructing and searching for directed cycles in the Wait-For Graph (WFG)',
      'By monitoring the ping latency between database cluster nodes',
      'By verifying that all SQL queries use parameterized queries',
    ],
    correctIndex: 1,
    correctExplanation: 'In a Wait-For Graph (WFG), nodes represent active transactions and directed edges Ti → Tj indicate that Ti is waiting for Tj to release a lock. A directed cycle in the WFG represents a circular dependency, indicating a deadlock.',
    optionExplanations: [
      'Disk storage capacity is an infrastructure metric, unrelated to circular lock dependencies.',
      'Searching for directed cycles in the Wait-For Graph is correct; if a cycle exists, transactions in the cycle are deadlocked and the DBMS must abort a victim.',
      'Network ping latency is a physical network health check, not deadlock detection.',
      'Parameterized queries prevent SQL injection, not concurrency deadlocks.',
    ],
  },
  {
    id: 'dbms-q5-05',
    unitId: 'unit-5',
    unitNumber: 5,
    unitTitle: 'UNIT V – Concurrency Control & Disk Storage',
    topic: 'Deadlock Prevention - Wait-Die Scheme',
    difficulty: 'Hard',
    conceptTag: 'Wait-Die Protocol',
    question: 'In the Wait-Die deadlock prevention scheme based on timestamps (where older transactions have smaller timestamps), what happens when transaction Ti requests a lock held by transaction Tj?',
    options: [
      'If Ti is older than Tj (TS(Ti) < TS(Tj)), Ti is allowed to WAIT; if Ti is younger than Tj (TS(Ti) > TS(Tj)), Ti DIES (aborts and restarts with its original timestamp)',
      'The younger transaction always preempts the older transaction and forces it to abort',
      'Both transactions are aborted immediately to prevent potential priority inversions',
      'Ti is allowed to wait indefinitely regardless of timestamps',
    ],
    correctIndex: 0,
    correctExplanation: 'Wait-Die is a non-preemptive scheme: If an older transaction needs a lock held by a younger one, it is allowed to WAIT. If a younger transaction requests a lock held by an older one, it DIES (aborts and restarts). Edges in the WFG can only point from older to younger, preventing cycles.',
    optionExplanations: [
      'Older waits, younger dies is correct: TS(Ti) < TS(Tj) => Ti waits; TS(Ti) > TS(Tj) => Ti dies.',
      'Younger preempting the older transaction is impossible in Wait-Die (it would cause cycles).',
      'Aborting both transactions is unnecessary and causes extreme thrashing.',
      'Waiting indefinitely would allow circular wait chains to form, causing deadlocks.',
    ],
  },
  {
    id: 'dbms-q5-06',
    unitId: 'unit-5',
    unitNumber: 5,
    unitTitle: 'UNIT V – Concurrency Control & Disk Storage',
    topic: 'Deadlock Prevention - Wound-Wait Scheme',
    difficulty: 'Hard',
    conceptTag: 'Wound-Wait Protocol',
    question: 'In the Wound-Wait deadlock prevention scheme based on timestamps, what happens when an older transaction Ti requests a lock held by a younger transaction Tj (TS(Ti) < TS(Tj))?',
    options: [
      'Ti dies and restarts with a new timestamp',
      'Ti preempts (wounds) Tj, forcing Tj to abort and release its lock so Ti can proceed',
      'Ti is blocked and forced to sleep until Tj finishes',
      'The DBMS converts all exclusive locks held by Tj into shared locks',
    ],
    correctIndex: 1,
    correctExplanation: 'Wound-Wait is a preemptive scheme: If an older transaction Ti requests a lock held by a younger transaction Tj, Ti WOUNDS Tj (preempts Tj by aborting it). If a younger transaction requests a lock held by an older one, it is allowed to WAIT.',
    optionExplanations: [
      'The older transaction never dies in Wound-Wait; older transactions have higher priority.',
      'Ti preempts/wounds Tj, forcing Tj to abort is correct; this preemptive approach minimizes aborts for transactions that are close to completion.',
      'Forcing older to wait on younger occurs in Wait-Die, not Wound-Wait.',
      'Locks cannot be arbitrarily downgraded to shared locks if write modifications have already occurred.',
    ],
  },
  {
    id: 'dbms-q5-07',
    unitId: 'unit-5',
    unitNumber: 5,
    unitTitle: 'UNIT V – Concurrency Control & Disk Storage',
    topic: 'Thomas Write Rule',
    difficulty: 'Hard',
    conceptTag: 'Thomas Write Rule',
    question: 'How does the Thomas Write Rule optimize the standard Timestamp Ordering protocol when transaction Ti attempts to write data item Q, but TS(Ti) < Write_TS(Q)?',
    options: [
      'It aborts Ti immediately and increments the global system clock',
      'It simply ignores/discards Ti\'s obsolete write operation and allows Ti to continue without aborting',
      'It rolls back all transactions that have read Q in the last hour',
      'It overwrites Q and forces all younger transactions to restart',
    ],
    correctIndex: 1,
    correctExplanation: 'Under the Thomas Write Rule, if Ti attempts to write Q and TS(Ti) < Write_TS(Q), Q has already been overwritten by a younger transaction. Since Ti\'s write would be immediately overwritten in an equivalent serial order, the DBMS safely ignores Ti\'s write without aborting Ti, generating view serializable schedules.',
    optionExplanations: [
      'Aborting Ti is the behavior of standard (strict) timestamp ordering, which Thomas Write Rule optimizes away.',
      'Ignoring/discarding Ti\'s obsolete write without aborting is correct; it recognizes that the obsolete value is never observed by any transaction.',
      'Rolling back all past readers is unnecessary and would violate transaction commit durability.',
      'Overwriting with an older value would corrupt the database state and violate serializability.',
    ],
  },
  {
    id: 'dbms-q5-08',
    unitId: 'unit-5',
    unitNumber: 5,
    unitTitle: 'UNIT V – Concurrency Control & Disk Storage',
    topic: 'Multiple Granularity Locking & Intention Locks',
    difficulty: 'Medium',
    conceptTag: 'Intention Locks (IS, IX)',
    question: 'In a Multiple Granularity Locking hierarchy (Database → Table → Page → Record), why are Intention Locks (such as IS and IX) placed on ancestor nodes before locking a record?',
    options: [
      'To back up the ancestor nodes to tape storage',
      'To indicate that a lower-level descendant node is locked, preventing other transactions from locking the entire ancestor node without traversing the whole tree',
      'To encrypt the table data against unauthorized external reads',
      'To compress page blocks in RAM',
    ],
    correctIndex: 1,
    correctExplanation: 'Intention locks (IS, IX) allow ancestor nodes to flag that explicit locking is being conducted at lower levels. When another transaction wants to lock the whole table in exclusive mode, it can check the table\'s intention lock instead of having to inspect every single record.',
    optionExplanations: [
      'Intention locks are concurrency synchronization flags, not backup mechanisms.',
      'Indicating lower-level descendant locking to avoid scanning the entire tree is correct, dramatically accelerating lock compatibility checks.',
      'Data encryption is handled by cryptographic storage layers, not intention lock protocols.',
      'Page compression is an I/O buffer management feature, not multiple granularity locking.',
    ],
  },
  {
    id: 'dbms-q5-09',
    unitId: 'unit-5',
    unitNumber: 5,
    unitTitle: 'UNIT V – Concurrency Control & Disk Storage',
    topic: 'Slotted Page Architecture',
    difficulty: 'Medium',
    conceptTag: 'Slotted Page Architecture',
    question: 'In physical database page organization, what does the Slotted-Page Header at the start of a disk block contain?',
    options: [
      'The source code of all stored procedures in the database',
      'An array of slot pointers containing the record offset and length for each record stored in the block',
      'A list of all user passwords and security hashes',
      'The CPU register cache dump from the last context switch',
    ],
    correctIndex: 1,
    correctExplanation: 'A Slotted Page contains a header with an array of slot entries. Each entry holds a pointer (byte offset) to where the record begins and the record\'s byte length. Slot entries grow from the front of the block, while records grow from the end of the block.',
    optionExplanations: [
      'Stored procedures are stored in the system catalog metadata tables, not inside individual data block headers.',
      'An array of slot pointers with record offset and length is correct; this allows variable-length records and defragmentation without breaking external tuple IDs (RID = PageID:SlotNumber).',
      'User credentials are encrypted and stored in secure catalog authentication tables.',
      'CPU register dumps are low-level kernel crash artifacts, not relational page architectures.',
    ],
  },
  {
    id: 'dbms-q5-10',
    unitId: 'unit-5',
    unitNumber: 5,
    unitTitle: 'UNIT V – Concurrency Control & Disk Storage',
    topic: 'RAID Storage Architecture',
    difficulty: 'Medium',
    conceptTag: 'RAID 5',
    question: 'Which RAID configuration provides block-level data striping with distributed parity across all member disks, allowing the array to survive any single-disk failure without creating a single dedicated parity disk bottleneck?',
    options: [
      'RAID 0 (Striping without parity)',
      'RAID 1 (Pure mirroring)',
      'RAID 4 (Block striping with dedicated parity disk)',
      'RAID 5 (Block striping with distributed parity)',
    ],
    correctIndex: 3,
    correctExplanation: 'RAID 5 stripes both data and parity blocks across all disks in the array (distributed parity). If any single disk fails, its data can be reconstructed using XOR operations with the remaining disks and parity blocks, avoiding the write bottleneck of a single dedicated parity disk (which was the drawback of RAID 4).',
    optionExplanations: [
      'RAID 0 provides striping only with zero fault tolerance; if any disk fails, all data is lost.',
      'RAID 1 mirrors every block onto duplicate disks (100% capacity overhead) rather than block parity striping.',
      'RAID 4 uses a single dedicated parity disk, which becomes a severe write bottleneck for every update.',
      'RAID 5 is correct because distributing parity across all disks balances the write load while providing single-disk fault tolerance.',
    ],
  },
];
